@model Question

@{
    ViewBag.Title = "AskQuestion";
}


<div class="row" style="margin:50px;padding-top:0px;">
    <div class="contact-form-warp">
        <form id="questionadd" class="contact-form">
            <h2>AskQuestion</h2>
            <div class="input-group mb-3">
                @Html.DropDownListFor(q => q.CategoryId, (SelectList)ViewBag.categorylist, "Select a Category")
            </div>
            <br />
            <div class="input-group mb-3">
                <div class="input-group-prepend">
                    <span class="input-group-text" id="inputGroup-sizing-default">Question</span>
                </div>
                <input name="Sharing" class="form-control" type="text" aria-label="Sizing example input" aria-describedby="inputGroup-sizing-default" required>
            </div>
            <br />
            <div class="input-group mb-3">
                <div class="input-group-prepend">
                    <span class="input-group-text" id="inputGroup-sizing-default">Explanation</span>
                </div>
                <input name="Explanation" class="form-control" type="text" aria-label="Sizing example input" aria-describedby="inputGroup-sizing-default" required>
            </div>
            <br />
            <div class="input-group mb-3">
                <button id="save" type="submit" formaction='@Url.Action("AskQuestion", "Home")' formmethod="post" class="btn btn-primary">Save</button>
            </div>
        </form>
    </div>
</div>


@section scripts{
    <script>
        
    </script>
}